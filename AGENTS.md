# AGENTS.md — 项目协作约定

> 本文件是 AI 助手接入本项目的快速上手指南。**任何一轮开发、任何一次代码改动前，先读本文件。**

---

## 一、项目概览

**家庭账户统计工具**（个人记账/账户统计，非每笔账单流水，而是不定期的家庭账户统计快照）。

- **纯前端**：静态 HTML + CSS + JS + LocalStorage，`file://` 直接打开，无需服务器
- **技术栈**：Vue 3（全局构建版，CDN 引入）+ 原生 JS + LocalStorage
- **用途**：作者个人使用，也是一份与AI协作开发的个人作品
- **当前版本**：V0.3（界面重构完成）
- **质量优先，速度其次**：宁可慢，不可糙

## 二、目录结构

```
family-financial-records/
├── html/                    # 主开发目录
│   ├── account.html         # 页面入口
│   ├── assets/account.css   # 样式
│   └── js/account.js        # 逻辑
├── html_single/             # 组装好的单文件版和mock示例数据
├── CHANGELOG.md             # 版本变更记录
└── README.md                # 项目主页说明
```

## 三、编码规范

### 注释规范
- **简洁风格**：`/* xxx */`、`// xxx`、`<!-- xxx -->`
- **禁止装饰符**：不要用 `=====`、`-----` 等符号装饰注释标题（尤其 JS 里的 `// ---- xxx ----` 一律写成 `// xxx`）
- **函数注释**：统一 `/** */` 风格
- **注释措辞稳定**：无关紧要的注释词汇一旦定稿就保持稳定，不随意增减
- **CSS 注释位置不随意挪动**：纯粹移动位置但内容未变的，保持原貌

### 文案与命名
- **中文与英文/数字之间不加多余空格**：避免 AI 味，比如 "避免AI味" 不要写成 "避免 AI 味"
- **代码变量名**：驼峰命名，语义清晰

## 四、提交规范

- **中文描述**，10-20 字概括本次改动，可用 `feat:`/`fix:`/`chore:` 等 `type:` 前缀
- **单改动点 / 简单小改动**：一行 message 即可
- **多个改动点**：标题 + 空行 + 紧凑多行正文（条目间无空行）
- **不自动 commit/push**：改好文件后给出 commit message 交作者审核

## 五、关键约定

### 存储 key（定死不变）
```
financial_account_records            # 记录数据
financial_account_bank_card_configs  # 银行卡配置
```

### 数据安全优先
- 底层数据结构不能留 bug：可能产生脏数据的问题必须在当前版本解决，不能留到后续
- 页面样式类"小尾巴"可以留作后续迭代，但数据正确性问题不能拖

## 六、下一步计划

- **V0.4**：目标计划板块（年收入、目标金额、目标日期、剩余时长计算）
