# 更新日志

---

## [V0.2] - 2026-09-17

### 新增

- **导入 / 导出 JSON**：一键导出完整数据（银行卡配置 + 账户记录）为 JSON 文件，文件名带日期；支持导入，便于定期备份与数据迁移
- **导出 CSV**：将账户记录导出为 CSV 文件，可用 Excel / 表格软件打开
- **对比差额列**：列表新增「对比差额」列，展示每条记录相对上一次统计的总余额变化，正数绿色带 `+`、负数红色带 `-`
- **银行卡禁用 / 启用**：可临时禁用某张银行卡，禁用后在「添加记录」时不再显示，历史数据保留、编辑时仍可见
- **Mock 示例数据**：新增 `data/account_mock.json` 示例数据，可一键导入快速预览完整功能

### 优化

- **代码结构拆分**：单文件拆分为 `account.html` + `assets/account.css` + `js/account.js`，样式与逻辑独立，便于阅读和维护
- **样式调整**：按钮组视觉分区、表格表头优化、弹窗样式细化、移动端适配完善
- **导出元信息**：导出 JSON 增加 `version` 与 `exportTime` 元信息，便于数据溯源

### 修复

- 修复对比差额列计算方向错误
- 修复分页后从第二页打开记录详情时，错误展示第一页数据的问题

---

## [V0.1] - 2026-09-13

### 新增

- **银行卡配置管理**：新增、编辑银行卡，可自定义显示名称与类型（存款 / 负债 / 两者兼备）
- **账户记录增删改查**：添加、编辑、删除记录；同一天不能重复录入；默认填充今日日期
- **金额展示**：千分位格式化；总余额按正负分色（正绿 / 负红）；存款与负债分别用不同颜色标识
- **备注字段**：记录支持填写备注，列表中两行截断显示
- **查看 / 编辑态分离**：点击日期打开记录弹窗默认只读，查看态展示汇总（总余额 / 总存款 / 总负债），点击「编辑」才可修改
- **数据列表分页**：每页 10 条，支持上一页 / 下一页
- **移动端适配**：窄屏下表格、弹窗、分页控件自动收敛，手机浏览友好
- **弹窗样式**：圆角卡片、阴影与淡入动画，输入框聚焦高亮
- **Toast 提示**：操作成功 / 失败均有轻量提示

### 说明

- 数据存储于浏览器 LocalStorage，存储 key 以 `financial_account_` 为前缀
